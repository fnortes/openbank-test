import styled from "@emotion/styled";
import WizardFooter from "./index";

export const StyledWizardFooter = styled(WizardFooter)`
  border-top: 1px solid ${(props) => props.theme.colors.lightGray};
`;
